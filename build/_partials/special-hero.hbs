<div class="special-hero {{ modifier_class }}">
  <div id="{{ heroId }}">

    {{#if brightcoveId}}
      <div class="special-hero__video">
        <div class="special-hero__video-inner bg-cover" style="background-image: url({{ background }});">
          <![if gt IE 8]>
          <video data-video-id="{{ brightcoveId }}" data-account="{{ brightcoveAccount }}" data-player="{{ brightcovePlayer }}" data-embed="{{ brightcoveEmbed }}" class="video-js" poster="{{ background }}" loop autoplay>
          </video>
          <script src="//players.brightcove.net/{{ brightcoveAccount }}/{{ brightcovePlayer }}_{{ brightcoveEmbed }}/index.min.js"></script>
          <![endif]>
        </div>
      </div>
    {{else}}

      {{#if video}}
        <div class="special-hero__video">
          <div class="special-hero__video-inner">
            <video autoplay="" loop="">
              <source src="{{ video }}" type="video/mp4">
              <img src="{{ background }}">
            </video>
          </div>
        </div>
      {{/if}}

    {{/if}}

    {{#if background}}
      <div class="special-hero__background">
        <div class="special-hero__background-inner" style="background-image: url({{ background }});">
        <!--[if lt IE 9]>
          <img class="special-hero__background-fallback" src="{{ background }}">
        <![endif]-->
        </div>
      </div>
    {{/if}}

    <div class="special-hero__content">
      <div class="special-hero__content-inner content-container">
        {{#if title}}
          <h1 class="special-hero__title">{{{ title }}}</h1>
        {{/if}}
        {{#if subtitle}}
          <h5 class="special-hero__subtitle">{{{ subtitle }}}</h5>
        {{/if}}
        {{#if content}}
          {{{ content }}}
        {{/if}}
      </div>
    </div>

  </div>

  {{#if revealContent}}
    <div class="reveal__content" id="{{ revealId }}">
      {{{ revealContent}}}
    </div>
  {{/if}}
</div>
